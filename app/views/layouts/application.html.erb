<!DOCTYPE html>
<html>
	<head>
	  <title>GFP - Gerente Financeiro Pessoal</title>
	  <%= stylesheet_link_tag    "application", :media => "all" %>
	  <%= javascript_include_tag "application" %>
	  <%= csrf_meta_tags %>
	</head>

	<body>
		<div class="container top">
			<table>
				<tr>
					<td><img src="/images/logo.png"/></td>
					<td><div>
				<h1>GFP</h1>
				<p class="lead">Gerente Financeiro Pessoal</p>
			</div></td>
				</tr>
			</table>	
			<div></div>
						
		</div>

		<div class="container body">		
				<% if session[:user] %>
					<br>Bem-vindo <%= session[:user].nome %>!
					<% if session[:last_login] %>
						<br>Seu último acesso foi em: <%= session[:last_login].strftime("%d/%m/%Y às %H:%M") %> 
					<% end %>	
					<%= link_to "Perfil", edit_usuario_path(session[:user]) %>
					<%= link_to "Sair", sessions_path, method: :delete %>
				<% end %>

			<% if flash[:notice] %>
				<%= flash[:notice] %>
			<% end %>
		
			<%= yield %>
		</div>

		<div class="container footer">
			<sub>Gerente Financeiro Pessoal - Copyright 2012</sub>
		</div>
	</body>
</html>